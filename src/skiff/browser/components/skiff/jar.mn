# Skiff chrome registration. Bakes the loader sys.mjs into omni.ja under
# the chrome://skiff/content/ namespace so BrowserGlue can import it via
# ChromeUtils.importESModule.

skiff.jar:
% content skiff %content/
  content/SkiffLoader.sys.mjs (SkiffLoader.sys.mjs)
