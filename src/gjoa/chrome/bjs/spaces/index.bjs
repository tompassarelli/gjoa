#lang beagle/js
(ns gjoa.spaces.index
  (:require [gjoa.spaces.manager :as mgr]
            [gjoa.spaces.visibility :as vis]))
(define-mode strict)

(js/export (def make-spaces :- Any (.-make-spaces mgr)))
(js/export (def is-in-active-space :- Any (.-is-in-active-space vis)))
