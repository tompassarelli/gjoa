# Gjoa chrome registration. Bakes the loader sys.mjs into omni.ja under
# the chrome://gjoa/content/ namespace so BrowserGlue can import it via
# ChromeUtils.importESModule.

gjoa.jar:
% content gjoa %content/
  content/GjoaLoader.sys.mjs (GjoaLoader.sys.mjs)
