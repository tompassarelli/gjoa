{
  "note": "The about:gjoa settings registry. gjoa's single settings home. Each setting is a live pref (present + reversible). Privacy profiles are CURATED presets — pick one and it flips a curated subset of the granular toggles; hand-editing any toggle switches you to Custom. Costs are MEASURED, characterized as real EGRESS endpoints, or honestly 'unmeasured' — never invented. Profile `sets` are kept in sync with GjoaLoader's PRIVACY-PRESETS by preflight Gate N. Firefox's own settings stay in Firefox Settings; this is gjoa's surface.",
  "privacy": {
    "title": "Privacy",
    "intro": "Pick a profile, or tune the individual toggles below (which switches you to Custom). Profiles escalate — each is a superset of the one before it.",
    "selector": {
      "pref": "gjoa.privacy.profile",
      "default": "balanced",
      "profiles": [
        { "id": "minimal", "title": "Minimal",
          "summary": "gjoa's privacy defaults — telemetry and phone-home off, Do-Not-Track on. Zero site breakage.",
          "sets": {
            "privacy.resistFingerprinting": false,
            "privacy.resistFingerprinting.letterboxing": false,
            "network.http.referer.XOriginPolicy": 0,
            "network.http.referer.XOriginTrimmingPolicy": 0,
            "browser.search.suggest.enabled": true,
            "browser.urlbar.suggest.searches": true,
            "extensions.update.enabled": true,
            "privacy.donottrackheader.enabled": true
          } },
        { "id": "balanced", "title": "Balanced",
          "summary": "Minimal + trims cross-site referers and turns off search / URL-bar suggestions (no keystroke leak to your search engine). Negligible breakage.",
          "sets": {
            "privacy.resistFingerprinting": false,
            "privacy.resistFingerprinting.letterboxing": false,
            "network.http.referer.XOriginPolicy": 0,
            "network.http.referer.XOriginTrimmingPolicy": 2,
            "browser.search.suggest.enabled": false,
            "browser.urlbar.suggest.searches": false,
            "extensions.update.enabled": true,
            "privacy.donottrackheader.enabled": true
          } },
        { "id": "librewolf", "title": "LibreWolf-Inspired",
          "asterisk": "A curated subset of LibreWolf's privacy DEFAULTS — not LibreWolf itself: its compile-time disables and ~20 C++ patches aren't here, and this changes none of gjoa's own added surface (the adblock classifier, content actors, about: pages).",
          "summary": "Balanced + resistFingerprinting (canvas / timezone / UA spoofing + window letterboxing), strict referer policy, and extension auto-update off. Real UX cost: RFP breaks some sites.",
          "sets": {
            "privacy.resistFingerprinting": true,
            "privacy.resistFingerprinting.letterboxing": true,
            "network.http.referer.XOriginPolicy": 2,
            "network.http.referer.XOriginTrimmingPolicy": 2,
            "browser.search.suggest.enabled": false,
            "browser.urlbar.suggest.searches": false,
            "extensions.update.enabled": false,
            "privacy.donottrackheader.enabled": true
          } }
      ]
    },
    "granular": [
      { "pref": "privacy.resistFingerprinting", "type": "bool", "title": "Resist fingerprinting (RFP)", "firefoxDefault": false,
        "help": "Spoofs canvas, timezone (UTC), and a generic UA to defeat fingerprinting. High UX cost — breaks canvas-reliant sites." },
      { "pref": "privacy.resistFingerprinting.letterboxing", "type": "bool", "title": "Letterboxing", "firefoxDefault": false,
        "help": "Pads the viewport to a generic size so window dimensions can't fingerprint you. Visible grey margins." },
      { "pref": "network.http.referer.XOriginPolicy", "type": "int", "title": "Cross-origin referer policy", "firefoxDefault": 0,
        "help": "0 = send full referer; 2 = send only on same-origin requests (strictest)." },
      { "pref": "network.http.referer.XOriginTrimmingPolicy", "type": "int", "title": "Cross-origin referer trimming", "firefoxDefault": 0,
        "help": "0 = full URL; 2 = trim cross-origin referer to scheme+host+port (no path/query leak)." },
      { "pref": "browser.search.suggest.enabled", "type": "bool", "title": "Search suggestions", "firefoxDefault": true,
        "help": "Off stops sending what you type to the search engine for suggestions." },
      { "pref": "browser.urlbar.suggest.searches", "type": "bool", "title": "URL-bar search suggestions", "firefoxDefault": true,
        "help": "Off stops URL-bar keystrokes reaching the search engine." },
      { "pref": "extensions.update.enabled", "type": "bool", "title": "Extension auto-update", "firefoxDefault": true,
        "help": "Off stops automatic add-on update checks (an egress + supply-chain consideration). You update manually." },
      { "pref": "privacy.donottrackheader.enabled", "type": "bool", "title": "Do-Not-Track header", "firefoxDefault": false,
        "help": "Sends DNT:1. Advisory only — most sites ignore it; gjoa's native blocking is what actually stops trackers." }
    ]
  },
  "sections": [
    { "id": "dark-mode-color", "title": "Dark mode & color",
      "intro": "Everything about how gjoa colors the web and dresses its own chrome — the dark-mode engine plus the gjoa theme — in one place. gjoa is dark-first: by default a bright white site (think YouTube) is darkened for you. Two groups below: Dark mode (how web pages are darkened) and Theme (gjoa's own window colors).",
      "subsections": [
        { "id": "darkmode", "title": "Dark mode",
          "intro": "Engine-level dark mode. Sites that ship their own dark theme can render it; the rest are inverted at style-resolution time (cached in the engine, no per-frame compositor filter). The layers stack, broadest to narrowest: the master switch → the Mode picker → the engine appearance knobs (Darkness, Text brightness, Scrim) → the three Per-site lists → gjoa's built-in curated per-site fixes. Each narrower layer overrides the one before it. Everything is right here — nothing lives in about:config.",
          "settings": [
            { "pref": "gjoa.darkmode.enabled", "type": "bool", "title": "Dark mode (master switch)", "default": true,
              "help": "The on/off for gjoa's whole dark-mode engine. When ON, the Mode below decides how each page is darkened. When OFF, gjoa does nothing to page colors — every site renders in its own authored colors (usually light), exactly like vanilla Firefox.",
              "examples": [
                { "label": "ON", "detail": "Reddit, YouTube, and a plain white blog are all darkened according to the Mode you pick below." },
                { "label": "OFF", "detail": "YouTube is bright white, Reddit is its normal light theme — gjoa stays out of the way." }
              ] },
            { "pref": "gjoa.darkmode.mode", "type": "enum", "title": "Mode — how pages are darkened", "default": "system",
              "options": [
                { "value": "off", "label": "Off — every site in its own colors" },
                { "value": "auto", "label": "Only sites that already have a dark theme (gentlest)" },
                { "value": "system", "label": "Match my OS theme (current default)" },
                { "value": "hybrid", "label": "Always dark — keep each site's own dark theme where it has one" },
                { "value": "engine", "label": "Always dark — force every site uniform (most consistent)" },
                { "value": "filter", "label": "Always dark — legacy whole-page invert (advanced)" }
              ],
              "help": "The core choice, ordered gentlest → most aggressive. The first three respect the web (sites stay light unless they opt in or your OS is dark); the last three force dark everywhere, differing only in HOW. If you want the uniform, Dark-Reader-style dark on every site (the most consistent result), pick 'engine'. Only matters while the master switch is ON.",
              "examples": [
                { "value": "off", "label": "off", "detail": "Identical to turning the master switch off: every site in its own (usually light) colors. Exists so the Mode can be parked without losing your other settings." },
                { "label": "system", "detail": "Follow your operating system. Dark OS ⇒ pages go dark; light OS ⇒ sites render as-authored. Caveat: sites that ignore the OS signal (e.g. YouTube) stay light, and sites with a very dark native theme (e.g. Reddit) stay near-black — the Darkness slider does NOT move these two cases." },
                { "label": "hybrid", "detail": "Always dark regardless of your OS. A site that ships its own dark theme uses that theme; every other site is engine-inverted. Best blend of fidelity and coverage." },
                { "label": "auto", "detail": "Only sites that already have their own dark theme go dark; nothing else is inverted. A plain white blog stays white. The gentlest option." },
                { "label": "engine", "detail": "Forces EVERY site to its light theme first, then the engine inverts all colors into your Darkness band. This is the consistent, Dark-Reader-style mode: Reddit, YouTube, anything — all become dark at exactly your Darkness setting. Pick this if you want uniform dark everywhere." },
                { "label": "filter", "detail": "Legacy whole-page compositor invert. Superseded by engine; kept as an advanced fallback. Inverts the entire page (re-inverting images so photos look right), but heavier than engine. Use only if engine misbehaves on a specific page." }
              ] },
            { "pref": "gjoa.darkmode.invert.bgLightness", "type": "int", "title": "Darkness", "default": 16, "min": 0, "max": 100, "slider": true,
              "help": "How dark inverted (engine-darkened) pages get — the page-background lightness floor every darkened site lands on. 0–100; LOWER = darker. The default 16 is a deep low-grey (about #0d0d0d), deliberately not pure black. Raise it if dark feels too stark; lower it toward 8–10 for near-black.",
              "examples": [
                { "label": "16 (default)", "detail": "Page backgrounds settle on a deep #0d0d0d — dark and uniform without crushing to black." },
                { "label": "8", "detail": "Pushes backgrounds toward near-black (#020202) — maximum contrast, can feel harsh." },
                { "label": "30", "detail": "A lighter, dim-grey page (~#2a2a2a) — gentler if pure-dark feels too stark." }
              ],
              "note": "Only affects pages the engine actually inverts. A site keeping its OWN dark theme (Reddit, or any native-dark site under system/hybrid) ignores this slider — its darkness comes from the site, not gjoa." },
            { "pref": "gjoa.darkmode.invert.fgLightness", "type": "int", "title": "Text brightness ceiling", "default": 92, "min": 0, "max": 100, "slider": true,
              "help": "The companion to Darkness: the brightest result the inverter will produce, i.e. how light the text and light surfaces get on a darkened page. 0–100; HIGHER = brighter text. The default ~92 is a soft off-white (about #e4e4e4), deliberately not pure #fff (full white glares on a dark backdrop).",
              "examples": [
                { "label": "92 (default)", "detail": "Body text renders as a comfortable off-white — high legibility without glare." },
                { "label": "100", "detail": "Pure-white text — maximum contrast, sharper but harsher on a dark page." },
                { "label": "80", "detail": "Dimmer, grey-ish text — softer, lower-contrast; good in a very dark room." }
              ],
              "note": "Most people only ever touch Darkness; this is the advanced ceiling for the same OKLCH lightness band. Only applies to engine-inverted pages." },
            { "pref": "gjoa.darkmode.scrim.alpha", "type": "int", "title": "Photo scrim strength", "default": 140, "min": 0, "max": 255, "slider": true,
              "help": "On an inverted page, full-bleed background photos (a hero banner) can't be inverted without looking wrong, so gjoa lays a translucent black scrim over them — this keeps any preserved (un-inverted) text readable against the still-bright photo. 0–255; HIGHER = darker scrim. 0 turns the scrim off entirely.",
              "examples": [
                { "label": "140 (default)", "detail": "A site with a bright hero image gets a moderate dark wash so white caption text over it stays readable." },
                { "label": "0", "detail": "No scrim — hero photos render at full brightness; text over them may wash out." },
                { "label": "230", "detail": "Heavy near-opaque wash — photos are strongly dimmed, text maximally legible." }
              ] },
            { "pref": "gjoa.darkmode.image-analysis.enabled", "type": "bool", "title": "Per-image refinement (experimental)", "default": false,
              "help": "An optional second pass that rasterizes each visible background image, classifies its brightness, and refines it individually (hides large light backdrops, inverts small dark icons, etc.). OFF by default — it is unverified polish layered on top of the scrim, which already handles the common case.",
              "examples": [
                { "label": "OFF (default)", "detail": "The engine scrim handles background images — correct for nearly every page, zero extra work." },
                { "label": "ON", "detail": "gjoa additionally inspects each image; may improve odd icon/logo edge cases, but can occasionally misjudge an image. Turn on only if a specific site's images look wrong." }
              ] },
            { "pref": "gjoa.darkmode.user.force-native", "type": "string", "title": "Per-site — keep the site's own theme", "default": "",
              "help": "Comma-separated domains gjoa should NEVER invert — it leaves them in their own authored colors (their native dark theme if they have one, otherwise light). Use this for a site whose own dark theme you prefer over gjoa's inversion.",
              "examples": [
                { "label": "github.com, news.ycombinator.com", "detail": "GitHub keeps its own dark theme; HN renders as-authored — gjoa won't touch either, while every other site still follows your Mode." },
                { "label": "(empty — default)", "detail": "No exceptions; every site follows the Mode above." }
              ],
              "note": "Overrides gjoa's automatic per-site decision. Lowest of the three per-site rules: 'disable' wins, then 'force dark', then this." },
            { "pref": "gjoa.darkmode.user.force-invert", "type": "string", "title": "Per-site — always force gjoa dark", "default": "",
              "help": "Comma-separated domains gjoa should ALWAYS engine-invert into your Darkness band — even if the site ships its own dark theme. Use this when a site's native dark theme is ugly or inconsistent and you'd rather have gjoa's uniform dark.",
              "examples": [
                { "label": "reddit.com", "detail": "Instead of Reddit's near-black native theme, gjoa inverts it to your Darkness setting (e.g. #0d0d0d), consistent with every other inverted site." },
                { "label": "(empty — default)", "detail": "No forced sites; gjoa decides per the Mode above." }
              ],
              "note": "Beats 'keep the site's own theme', but is itself overridden by 'disable' below." },
            { "pref": "gjoa.darkmode.user.off", "type": "string", "title": "Per-site — disable dark mode entirely", "default": "",
              "help": "Comma-separated domains where gjoa does NOTHING — the page renders exactly as authored (usually light), as if dark mode were off, but only for these sites. Use this for a site where any darkening looks broken (design tools, maps, color-critical work).",
              "examples": [
                { "label": "figma.com, maps.google.com", "detail": "Design / map tools render in true colors, untouched, while every other site stays dark." },
                { "label": "(empty — default)", "detail": "No sites excluded." }
              ],
              "note": "Highest-precedence per-site rule — beats both lists above." },
            { "pref": "gjoa.darkmode.normalize.enabled", "type": "bool", "title": "Contrast guard (experimental)", "default": false,
              "help": "A final readability pass: after inversion settles, gjoa measures real text against its real background and re-tones any text that would fall below a legibility floor — the 'never unreadable, no black-on-black' backstop. OFF by default pending full build verification; the engine inversion already yields readable contrast in the common case.",
              "examples": [
                { "label": "OFF (default)", "detail": "Engine inversion handles contrast — correct for nearly every page, zero extra work." },
                { "label": "ON", "detail": "gjoa additionally re-tones any low-contrast text it detects. Turn on if a darkened site has hard-to-read text." }
              ] },
            { "pref": "gjoa.darkmode.normalize.floor", "type": "int", "title": "Contrast guard strictness", "default": 45, "min": 0, "max": 100, "slider": true,
              "help": "How strict the Contrast guard is, in APCA Lc (perceptual contrast). HIGHER = stricter (more text gets re-toned). ~45 ≈ large / secondary text; raise toward 60 for stricter body-text contrast. Only matters when the Contrast guard above is ON.",
              "examples": [
                { "label": "45 (default)", "detail": "Catches genuinely illegible text without over-correcting." },
                { "label": "60", "detail": "Stricter — enforces stronger body-text contrast, re-tones more aggressively." }
              ] }
          ] },
        { "id": "theme", "title": "Theme",
          "intro": "gjoa's OWN window chrome — the toolbar, tabs, and menus — not web-page colors. One OKLCH token system, fed by a base16 palette. Pick gjoa's canonical look, a curated variant, or dissolve into your desktop. No restart needed.",
          "settings": [
            { "pref": "gjoa.theme.variant", "type": "enum", "title": "gjoa theme", "default": "polar-night",
              "options": [
                { "value": "polar-night", "label": "Polar Night — gjoa's canonical face (default)" },
                { "value": "nord", "label": "Nord — the Arctic-cousin variant" },
                { "value": "system", "label": "System — match my desktop theme" }
              ],
              "help": "Which palette paints gjoa's chrome. Changes apply live.",
              "examples": [
                { "label": "Polar Night", "detail": "gjoa's signature: the deep polar-night sea, arctic-ice surfaces, aurora-cyan accent. The default identity." },
                { "label": "Nord", "detail": "The well-known Nord palette, normalized through gjoa's same token system — a cooler, softer cousin." },
                { "label": "System", "detail": "Routes gjoa's chrome colors through your desktop / Firefox theme (stylix, an installed theme, or OS colors) so the browser dissolves into your setup. This also flips the gjoa.theme.useSystem switch below." }
              ] },
            { "pref": "gjoa.theme.useSystem", "type": "bool", "title": "Use system / desktop colors", "default": false,
              "help": "Routes the gjoa palette through Firefox's theme variables (stylix, an installed theme, OS colors) instead of gjoa's own. The 'System' theme above turns this ON; any other variant turns it OFF — so normally you just pick the theme above and leave this alone. Exposed here for transparency and manual control.",
              "examples": [
                { "label": "OFF (default)", "detail": "gjoa paints its own canonical chrome colors (Polar Night or Nord)." },
                { "label": "ON", "detail": "gjoa borrows your system/Firefox theme colors — equivalent to choosing the 'System' theme above." }
              ] }
          ] }
      ] },
    { "id": "content-blocking", "title": "Content blocking", "intro": "Native (adblock-rust) ad + tracker blocking.",
      "settings": [
        { "pref": "gjoa.contentblock.enabled", "type": "bool", "title": "Ad / tracker blocking", "default": true },
        { "pref": "gjoa.contentblock.user.allow-hosts", "type": "string", "title": "Allow-list (hosts)", "default": "",
          "help": "Comma-separated hosts to exempt from blocking (your own allow-list). Empty = block everywhere." },
        { "pref": "gjoa.contentblock.scriptlets.listDriven.enabled", "type": "bool", "title": "List-driven scriptlets", "default": false,
          "help": "SECURITY: off = only curated baseline scriptlets run; on allows arbitrary +js() injection from your filter lists. Leave off unless you trust your lists.",
          "measurement": { "method": "cve-surface", "confidence": "moderate", "basis": "A security gate (finding F2), not a perf knob." } }
      ] },
    { "id": "reversible", "title": "Reversible features (debloat)",
      "intro": "Firefox subsystems gjoa disables by default - present and reversible. gjoa never deletes a capability; it parks it behind a knob. These are EGRESS/feature toggles, not memory knobs - so the honest cost is the network endpoint each would re-contact if you toggle it on (documented Firefox behaviour), not an invented perf number. Idle-memory deltas remain unmeasured; they are not the relevant axis here.",
      "settings": [
        { "pref": "toolkit.telemetry.enabled", "type": "bool", "title": "Telemetry", "default": false, "firefoxDefault": true,
          "measurement": { "method": "egress", "endpoint": "incoming.telemetry.mozilla.org", "confidence": "documented", "basis": "Firefox submits telemetry pings to incoming.telemetry.mozilla.org. Off in gjoa, so it never fires; toggling on re-enables that egress. Idle CPU/memory of the telemetry subsystem is unmeasured (not the relevant cost)." } },
        { "pref": "datareporting.healthreport.uploadEnabled", "type": "bool", "title": "Health Report upload", "default": false, "firefoxDefault": true,
          "measurement": { "method": "egress", "endpoint": "incoming.telemetry.mozilla.org", "confidence": "documented", "basis": "Firefox Health Report uploads to incoming.telemetry.mozilla.org. The cost is the data sent, not memory." } },
        { "pref": "extensions.pocket.enabled", "type": "bool", "title": "Pocket", "default": false, "firefoxDefault": true,
          "measurement": { "method": "egress", "endpoint": "spocs.getpocket.com, getpocket.cdn.mozilla.net", "confidence": "documented", "basis": "Pocket fetches recommendations/sponsored content from getpocket endpoints (surfaced on the stock new-tab, which gjoa replaces anyway). Feature toggle; no saving exists for a feature that was never running." } },
        { "pref": "identity.fxaccounts.enabled", "type": "bool", "title": "Firefox Accounts (Sync)", "default": false, "firefoxDefault": true,
          "measurement": { "method": "feature", "endpoint": "accounts.firefox.com", "confidence": "documented", "basis": "Disables Sync (accounts.firefox.com). Feature loss, not a measured perf win - egress only occurs if you actively sign in." } },
        { "pref": "browser.discovery.enabled", "type": "bool", "title": "Add-on discovery (TAAR)", "default": false, "firefoxDefault": true,
          "measurement": { "method": "egress", "endpoint": "services.addons.mozilla.org", "confidence": "documented", "basis": "The add-ons recommendation service (TAAR) sends usage signals to services.addons.mozilla.org. Egress vector; memory cost unmeasured." } },
        { "pref": "app.normandy.enabled", "type": "bool", "title": "Normandy remote experiments", "default": false, "firefoxDefault": true,
          "measurement": { "method": "egress", "endpoint": "normandy.cdn.mozilla.net", "confidence": "documented", "basis": "Normandy pulls remote experiment/recipe definitions from normandy.cdn.mozilla.net and can flip prefs remotely. Disabling it removes both that egress and remote mutation of your build." } },
        { "pref": "browser.contile.enabled", "type": "bool", "title": "Sponsored top sites (Contile)", "default": false, "firefoxDefault": true,
          "measurement": { "method": "egress", "endpoint": "contile.services.mozilla.com", "confidence": "documented", "basis": "Sponsored-tile placement is fetched from contile.services.mozilla.com. No functional cost (custom new-tab); the cost is the egress." } }
      ] },
    { "id": "layout", "title": "Layout",
      "intro": "How gjoa arranges tabs and chrome.",
      "settings": [
        { "pref": "gjoa.layout.dynamicOrientation", "type": "bool", "title": "Dynamic orientation (width-responsive tabs)", "default": true,
          "measurement": { "method": "feature", "basis": "Auto-switches to horizontal tabs on a narrow / half-width window and vertical (sidebar) tabs on a wide one, live as the window resizes. No egress; pure layout." } }
      ] }
  ],
  "links": [
    { "title": "Sovereignty - egress proof", "url": "about:sovereignty",
      "desc": "The source-derived list of every point this build reaches the network, checked against the build you're running. A proof, not a setting." },
    { "title": "Raw preferences", "url": "about:config",
      "desc": "Every gjoa.* and Firefox pref, raw. The expert escape hatch - each setting above shows its pref name to search here." }
  ]
}
