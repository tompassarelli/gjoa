# Pinned fram ref — the FROZEN fram commit gjoa's projector/codegraph tooling
# (tools/projector/*) resolves against, NOT live ~/code/fram. gjoa depends on fram
# ONLY for the code-as-claims projector (CI Gate C), never for the browser build.
# This full object ID names an immutable checkout at
# ~/code/fram/pins/<full-object-id>, with a same-name .pin sidecar naming Gjoa as
# its consumer. Local tooling derives that path from this file; CI may supply an
# explicit FRAM_PIN_ROOT checkout of the same object. To advance, create the new
# detached content-addressed pin and sidecar, then update this file. Never change
# the HEAD or bytes of an existing pin.
969bdc1d2b33a441629011f78b2b77c8f5cff759
