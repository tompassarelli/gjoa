{
  "$schema": "gjoa provenance identity (generated — do not edit by hand)",
  "note": "L4 baked-provenance identity. `digest` is one sha256 over EVERY locked blob OID + hunk hash in configs/upstream-provenance.json — the supply-chain coordinate proving the binary was built against unmodified Mozilla blobs. BUILD-GATED: stamping this into the binary's version/provenance metadata happens at compile time (not done here — no build). Regenerate with `provenance lock`; a mismatch vs the lock fails `check`.",
  "generator": "tools/prep/upstream-provenance.bjs (lock)",
  "baseline": "152.0.6",
  "tag": "FIREFOX_152_0_6_RELEASE",
  "digest": "227d3a0a66fe12af0ab60f3d9212cad6147dce4a72e7ea994af89b27fd4217db",
  "patches": 10,
  "files": 39,
  "hunks": 69
}
