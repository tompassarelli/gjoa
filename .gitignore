# mach build artifacts (mozilla-central source + per-build state)
engine/
.dotbuild/
firefox-*/
__pycache__/
dist/
.beagle-out/
.beagle-tools/
.beagle-pin-collects/
target/
mozconfig

# Bun / Node
node_modules/

# Nix dev shells / direnv
.devenv/
.direnv/

# Nix build output symlink
result
result-*
gjoa-portable

# Local build/test logs
artifacts/

# Build acceleration caches
.sccache/

# Gjoa prep tool source cache (downloaded firefox tarballs)
.cache/

# Local measurement telemetry (test:report / test:profile read this) — run-local
# data, not tracked; the README documents the dir + survives a fresh clone.
metrics/*
!metrics/README.md

# IDE / OS
.vscode/
.DS_Store

# Distribution artifacts
*.mar
*.dmg
*.tar.bz2
.beagle/
gjoa-arx

# Private, never-commit strategy/vision docs
private-docs/

# private security audit ledger — never tracked
tools/security/audit-ledger.bjs

# dev launcher auto-sync stamp (gjoa dev)
.gjoa-sync-stamp

# Build ledger moved to private-docs/build-logs/ (can leak nix paths / internal detail)
BUILD-LEDGER.md
